"""静态 Prompt 模板 - P0/P1/P2 分阶段实现

本模块定义了所有静态 Prompt 模板，包括：
- System Prompt 相关：角色定义、用户画像、Skill Registry、Few-shot
- Skill Protocol：4 条使用约定
- 使用指南：输出格式规范
- 内部操作：压缩器、HIL、错误恢复

按优先级分为：
- P0: 基础角色定义、用户画像模板、HIL 确认、错误恢复
- P1: Skill Registry 元数据、静态 Few-shot、压缩器
- P2: 动态 Few-shot（预留）
"""

# =============================================================================
# P0: 基础角色定义
# =============================================================================

ROLE_TEMPLATE = """你是一个专业的多工具 AI 助手，能够自主规划并执行复杂任务。

## 核心能力
- 实时信息搜索（web_search）
- 数据分析（csv_analyze）
- 合同流程查询
- 文件操作（read_file）

## 行为准则
- 每次行动前先推理，说明为什么选择当前工具
- 遇到不确定信息，优先搜索而不是猜测
- 最终答案使用 Markdown 格式
"""


# =============================================================================
# P0: 用户画像模板（动态注入）
# =============================================================================

USER_PROFILE_TEMPLATE = """
## 用户画像
{preferences}
"""


# =============================================================================
# P1: Skill Registry 元数据
# =============================================================================

SKILL_REGISTRY_TEMPLATE = """
## 可用 Skills
{skills_list}

激活方式：当任务匹配某个 skill 的 description 时，使用 read_file 工具读取对应的 SKILL.md
"""


# =============================================================================
# P1: 静态 Few-shot
# =============================================================================

STATIC_FEW_SHOT = """
## 示例对话（演示推理方式）

示例 1：
用户：查合同123的签署进度
思考：用户需要查询指定合同的状态，直接使用 contract_status 工具
操作：contract_status(contract_id="123")
观察：合同123已有2/3方签署，待签：财务总监（截止 2026-03-20）
回复：合同123进度：2/3 已签署，财务总监需在3月20日前完成签署。

示例 2：
用户：批量提醒所有逾期未签合同的签署方
思考：这是批量操作，需先确认范围再执行，必须触发人工确认
操作：query_overdue_contracts()
观察：15 份逾期合同，23 位签署方
HIL：此操作将向 23 位签署方发送提醒邮件，请确认是否继续？
"""


# =============================================================================
# P1: Skill Protocol - 四个约定
# =============================================================================

SKILL_PROTOCOL = """
## Skill 使用协议

当你需要使用特定技能时，遵循以下约定：

1. **识别约定**：当用户请求匹配某个 skill 的 description 时，在本次 ReAct 循环中激活该 skill。
2. **调用约定**：使用 read_file 工具读取 skill 的 file_path 获取完整内容。
3. **执行约定**：严格按照 SKILL.md 中的 Instructions 执行，遵循 Examples 的格式。
4. **冲突约定**：同一 turn 内只激活一个 skill，避免 Token 消耗过大。
5. **手动指定约定**：当用户消息以 [Skill: <name>] 开头时，表示用户明确指定使用该 skill。
   你必须优先通过 read_file 工具加载该 skill 的完整内容，再执行用户任务，不得跳过此步骤。
"""


# =============================================================================
# P0: 使用指南 - 输出格式规范
# =============================================================================

USAGE_GUIDE = """## 使用指南
1. 首先理解用户需求
2. 判断是否需要激活某个 skill（参考 skill 的 description）
3. 如果需要，使用 read_file 读取对应的 SKILL.md
4. 按 skill 的 Instructions 执行任务
5. 如果不需要 skill，直接使用可用工具完成任务

## 重要
- 不要编造信息
- 保持回答简洁但完整
- send_email 操作需要用户确认后才会执行
"""


# =============================================================================
# P2: 动态 Few-shot（预留）
# =============================================================================

# TODO: P2 实现动态 Few-shot 检索器
# 需要向量存储 + 语义检索


# =============================================================================
# 内部操作 Prompt - 系统级 LLM 调用
# =============================================================================

# -----------------------------------------------------------------------------
# 压缩 Prompt（P1 实现）
# -----------------------------------------------------------------------------

COMPRESSOR_PROMPT = """
请将以下对话历史压缩成简洁摘要（控制在 400 Token 以内）。

【必须保留】
· 用户的核心任务目标和意图
· 已确认的关键事实
· 工具调用结果及其核心含义
· 当前任务进度和尚未完成的事项

【可以省略】
· 闲聊、礼貌用语、重复确认
· 格式排版相关的指令

[对话历史]
{messages}

摘要：
"""


# -----------------------------------------------------------------------------
# HIL 确认模板（P0 实现）
# -----------------------------------------------------------------------------

HIL_CONFIRM_TEMPLATE = """
⚠️ 即将执行以下操作，请确认：

操作类型：{action_type}
影响范围：{scope_description}
具体内容：{action_detail}
预期结果：{expected_result}

[✅ 确认执行]   [❌ 取消操作]
"""


# -----------------------------------------------------------------------------
# 错误恢复规则（P0 实现）
# -----------------------------------------------------------------------------

ERROR_RECOVERY_RULES = """
工具调用失败时的处理规则：
1. 分析错误类型，修改参数后重试（最多 2 次）
2. 2 次重试后仍失败，切换备用方案或告知用户限制原因
3. 禁止使用相同参数重复调用（防死循环）
"""


# -----------------------------------------------------------------------------
# Few-shot 压缩示例（P1）
# -----------------------------------------------------------------------------

COMPRESSOR_FEW_SHOT = """
【示例 1：简单查询】

输入：
用户：茅台股价是多少？
助手：我帮您查询茅台的最新股价。
助手：[调用 web_search(query="茅台股价")]
工具：茅台（600519）今日收盘价 1680 元，涨幅 +1.2%。
助手：茅台今日股价为 1680 元，上涨 1.2%。

输出：
用户查询茅台股价，获知今日收盘价 1680 元（+1.2%）。

【示例 2：多轮任务】

输入：
用户：帮我分析合同123的签署情况。
助手：我来查询合同123的签署进度。
助手：[调用 contract_status(contract_id="123")]
工具：合同123已有2/3方签署，待签：财务总监（截止 2026-03-20）。
用户：那合同456呢？
助手：[调用 contract_status(contract_id="456")]
工具：合同456已完成签署，归档日期 2026-03-15。
用户：帮我提醒逾期合同的签署方。
助手：我先查询有哪些合同逾期未签。
助手：[调用 query_overdue_contracts()]
工具：发现 15 份逾期合同，涉及 23 位签署方。

输出：
用户分析了3份合同：合同123待财务总监签署（截止3/20），合同456已完成，另有15份合同逾期涉及23位签署方需提醒。
"""


# =============================================================================
# 模板导出
# =============================================================================

__all__ = [
    "ROLE_TEMPLATE",
    "USER_PROFILE_TEMPLATE",
    "SKILL_REGISTRY_TEMPLATE",
    "STATIC_FEW_SHOT",
    "SKILL_PROTOCOL",
    "USAGE_GUIDE",
    "COMPRESSOR_PROMPT",
    "HIL_CONFIRM_TEMPLATE",
    "ERROR_RECOVERY_RULES",
    "COMPRESSOR_FEW_SHOT",
]
